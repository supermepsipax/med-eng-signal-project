function selected_features = select_features(features, labels)
%% Select features - placeholder
% Students should implement feature selection
fprintf('Feature selection: Using all features (students should implement selection)\n');
selected_features = features;
end
